<Window x:Class="auth_app.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login"
        Height="250"
        Width="350">

    <Grid Margin="20">

        <StackPanel>

            <TextBlock Text="Username"/>

            <TextBox
                Margin="0,5,0,15"
                Text="{Binding Username,
                       UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Text="Password"/>

            <PasswordBox
                x:Name="PasswordBox"
                Margin="0,5,0,15"/>

            <Button
                Content="Login"
                Height="35"
                Click="Login_Click"/>

        </StackPanel>

    </Grid>

</Window>